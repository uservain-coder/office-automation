# 办公自动化作品集依赖
# 安装：pip install -r requirements.txt

pandas>=2.0        # 数据处理（脚本A：Excel 批量处理）
openpyxl>=3.0      # Excel 读写引擎（脚本A/B/D）
pdfplumber>=0.10   # PDF 文本/表格提取（脚本B）
python-docx>=0.8   # Word 文档处理（可选扩展）
reportlab>=4.0     # 工资条 PDF 输出（脚本D，可选）
tqdm>=4.0          # 命令行进度条（脚本A）
